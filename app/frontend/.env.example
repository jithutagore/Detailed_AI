# Copy to .env.local (gitignored) to override. Vite only exposes vars prefixed VITE_ to the client.

VITE_API_BASE_URL=/api

# Dev-server only: where the /api proxy forwards to (see vite.config.ts). Not bundled into the client.
VITE_PROXY_TARGET=http://127.0.0.1:8000
